<!--
  Shared auth-card emblem: the Suprnova logo centered at the top of every
  auth card (Login, Register, ForgotPassword, ResetPassword, VerifyEmail).
  Rendered unclipped and undecorated — the mark is the brand. Decorative —
  each card's heading carries the accessible context, so the image is
  hidden from assistive tech.
-->
<img
  src="/favicon-32x32.png"
  alt=""
  width="32"
  height="32"
  class="mx-auto size-10"
/>
